# Config temporaire SANS SSL — utilisée uniquement pour
# obtenir le certificat Let's Encrypt la première fois.
# Remplacée par nginx.conf après le certbot.

server {
    listen 80;
    listen [::]:80;
    server_name studypilot.tisepse.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass         http://studypilot:3000;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        client_max_body_size 10M;
    }
}
